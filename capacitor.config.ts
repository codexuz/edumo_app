import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uz.edumo.school',
  appName: 'Edumo_School',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
